# Bloqueia acesso HTTP direto a qualquer arquivo em app/
# O roteador (index.php na raiz) faz include/require normalmente via sistema de arquivos
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
